# AI使能的无线鲁棒SVD算子 - 依赖包列表 (CUDA GPU版本)

# 深度学习框架 - CUDA版本
# 注意：请根据你的CUDA版本选择对应的PyTorch版本
# 安装方法见下方说明

# 数值计算
numpy>=1.21.0

# 进度条显示
tqdm>=4.64.0

# 数据可视化
matplotlib>=3.5.0

# 系统监控（可选）
psutil>=5.9.0

# 科学计算（可选）
scipy>=1.9.0

# 数据分析（可选）
pandas>=1.4.0

# 配置文件处理（可选）
pyyaml>=6.0

# 内存优化（可选）
memory-profiler>=0.60.0

# ===== PyTorch CUDA 安装说明 =====
# 
# 请根据你的CUDA版本手动安装PyTorch：
#
# 查看CUDA版本：
#   nvidia-smi
#
# CUDA 11.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# CUDA 12.4:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# 如果不确定CUDA版本，可以安装CPU版本然后升级：
#   pip install torch torchvision torchaudio
#   # 然后根据需要升级到CUDA版本